# LibUDS artifacts

This folder contains **local run outputs** (captures, logs, reports).

- `capture/` — PCAPs + generated HTML session reports (`run_capture.sh`)
- `integration/` — C-to-C integration test logs (`run_integration_test.sh`)
- `tests/` — ad-hoc test outputs captured during verification
- `ci/` — archived CI logs (if present)

