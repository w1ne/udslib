# LibUDS Zephyr Server Configuration for native_sim (Fallback Mode)

# Enable CAN subsystem
CONFIG_CAN=y
CONFIG_CAN_NATIVE_LINUX=y

# Enable LibUDS with Fallback Transport (Internal ISO-TP)
CONFIG_LIBUDS=y
CONFIG_LIBUDS_TRANSPORT_FALLBACK=y

# Build settings
CONFIG_MAIN_STACK_SIZE=4096
