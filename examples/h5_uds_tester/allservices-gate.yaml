schema_version: "1.0"
inputs:
  env: "/home/andrii/projects/udslib-h5-gate/examples/h5_uds_tester/twonode-env.yaml"
limits:
  max_steps: 4000000
assertions:
  # 21/27 services (phases 1-4) after the F-9 + F-11 labwired fixes. 0x31/0x85/0x86
  # all pass. Remaining: 0x38 (F-10 incorrectLength), 0x83/0x84/0x87 (T6, not yet
  # wired). See the findings log.
  - memory_value: { node: tester, address: 0x20010000, expected_value: 0x337FEFD, size: 32 }
