schema_version: "1.0"
inputs:
  env: "/home/andrii/projects/udslib-h5-gate/examples/h5_uds_tester/twonode-env.yaml"
limits:
  max_steps: 4000000
assertions:
  # 14/27 services. 0x85/0x86 (and by extension 0x83/0x84/0x87) are descoped
  # pending the F-9 high-SID value-corruption fix — see the findings log.
  - memory_value: { node: tester, address: 0x20010000, expected_value: 0x303EFD, size: 32 }
